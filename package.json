{
  "name": "subscription-manager",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend & npm run dev --workspace=backend",
    "build": "npm run build --workspace=frontend && npm run build:pages --workspace=backend",
    "deploy": "npm run build --workspace=frontend && npm run deploy --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}